#include <string>
#include <iostream>
#ifndef _REVERSE_H
#define _REVERSE_H

class Reverse{
private:
    std::string reveroutput;
    std::string intstring;
    int check;
    int reint;
public:
    int reverseDigit(int value);//The function takes in an non-negative int value and reverses the digits using recursion. The “reversed” int is then returned.
    std::string reverseString(std::string words);// The function takes in a std::string and returns the reversed string
};
#endif
